package compat:many-arguments;

// Phase 0 compatibility fixture: many-argument exports (arity beyond the 1-3
// arguments used by the existing starling:js/api fixture), including a
// heterogeneous-type case.
world js-exports {
  export sum8: func(a: u32, b: u32, c: u32, d: u32, e: u32, f: u32, g: u32, h: u32) -> u32;
  export describe: func(flag: bool, count: u32, label: string, delta: s32, ready: bool, scale: u32) -> string;
}
