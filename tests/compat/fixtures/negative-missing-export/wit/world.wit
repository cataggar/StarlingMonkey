package starling:js;

// Phase 0 negative fixture: the world declares `phantom`, but component.js
// intentionally does not implement it. See manifest.json fixture
// "negative-missing-export" for how the two pipelines are documented to
// detect this (componentization-time for the pinned ComponentizeJS reference,
// call-time for the current Zig/WABT bridge).
interface api {
   present: func() -> u32;
   phantom: func() -> u32;
}

world js-exports {
  export api;
}
