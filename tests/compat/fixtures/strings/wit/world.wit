package starling:js;

// Phase 0 compatibility fixture: string arguments/results, including empty and
// multi-byte/astral Unicode content.
interface api {
   identity: func(value: string) -> string;
   concat: func(a: string, b: string) -> string;
   length: func(value: string) -> u32;
}

world js-exports {
  export api;
}
