{
  "private": true,
  "name": "starlingmonkey-feature-selection-reference",
  "version": "0.0.0",
  "description": "Opt-in reference mode for tests/feature-selection: runs disableFeatures/enableFeatures probes through the real pinned ComponentizeJS release for differential comparison against tests/feature-selection/reference/expected/import-surfaces.json. NOT part of the normal Zig/WABT build or test suite -- see README.md.",
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "devDependencies": {
    "@bytecodealliance/componentize-js": "0.21.0"
  },
  "scripts": {
    "run": "node probe.mjs"
  }
}
